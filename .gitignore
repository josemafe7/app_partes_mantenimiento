node_modules/
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

# Ningún archivo de entorno se sube, se llame como se llame (.env.produccion,
# .env.vercel…). Solo la plantilla, que no lleva valores reales.
.env
.env.*
!.env.example
.DS_Store

*.log

respaldos/

# El informe de la auditoría de seguridad es el mapa de los fallos: no se sube.
auditoria-seguridad.md
